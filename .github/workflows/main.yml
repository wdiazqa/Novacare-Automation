name: Cypress on BrowserStack with Local Tunnel + Percy

on: push
  

jobs:
  browserstack-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run Cypress tests on BrowserStack with local tunnel
        run: npx browserstack-cypress run
        env:
          PERCY_ENABLED: false
          BROWSERSTACK_USERNAME: ${{ secrets.BROWSERSTACK_USERNAME }}
          BROWSERSTACK_ACCESS_KEY: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}
          BROWSERSTACK_PROJECT_NAME: "NovoCare-Automation"
          BROWSERSTACK_BUILD_NAME: "${{ github.workflow }} - ${{ github.run_number }}"

  percy-visual-tests:
    runs-on: self-hosted # usa ubuntu-latest si no requiere VPN

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Chrome (required for Percy)
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Install dependencies
        run: npm install

      - name: Run Percy visual tests
        run: |
          echo "Iniciando Percy visual tests..."
          npx percy exec -- cypress run
        env:
          PERCY_ENABLED: true
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
          PERCY_BROWSER_EXECUTABLE: "/usr/bin/google-chrome"